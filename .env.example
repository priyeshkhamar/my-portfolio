# Set this to your production URL once you have a custom domain (no trailing slash).
# On Vercel, if you skip this, the app auto-uses your *.vercel.app domain.
# NEXT_PUBLIC_SITE_URL=https://your-domain.com
